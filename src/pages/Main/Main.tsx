import styles from './Index.module.scss';

import Header from '../Header/Header';

const Main = () => {
  return (
    <div className={styles.container}>
      <Header />
    </div>
  );
};

export default Main;
